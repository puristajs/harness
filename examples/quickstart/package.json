{
  "name": "@purista/quickstart",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run src",
		"start": "node dist/examples/quickstart/src/index.js",
		"lint": "npm run typecheck"
  },
  "dependencies": {
    "@purista/harness": "*",
    "@purista/harness-openai": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=24.15.0"
  }
}
