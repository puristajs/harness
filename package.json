{
	"name": "@purista/agent-harness-repo",
	"version": "0.0.0",
	"private": true,
	"description": "Self-hosted enterprise agent harness.",
	"type": "module",
	"engines": {
		"node": ">=24.15.0"
	},
	"workspaces": [
		"packages/*",
		"examples/*"
	],
	"overrides": {
		"express-rate-limit": "^8.5.2",
		"@grpc/grpc-js": "^1.14.4"
	},
	"scripts": {
		"lint": "npm run lint --workspaces --if-present",
		"typecheck": "npm run typecheck --workspaces --if-present",
		"test": "npm run test --workspaces --if-present",
		"test:coverage": "npm run test:coverage --workspaces --if-present",
		"test:contracts": "npm run test:contracts --workspace @purista/harness",
		"test:failure": "npm run test:failure --workspace @purista/harness",
		"test:integration": "npm run test:integration --workspace @purista/harness && npm run test --workspace @purista/quickstart",
		"test:types": "npm run test:types --workspace @purista/harness && npm run test:types --workspace @purista/harness-guardrails && npm run test:types --workspace @purista/harness-policy-opa",
		"skills:sync": "node scripts/sync-ai-harness-skill.mjs",
		"verify:architecture": "node --test scripts/package-boundaries.test.mjs scripts/check-decision-boundaries.test.mjs scripts/verify-decision-consumers.test.mjs scripts/check-release-metadata.test.mjs scripts/prepare-offline-verification-cache.test.mjs && node scripts/verify-capability-catalog.mjs && node scripts/check-decision-boundaries.mjs",
		"verify:release-metadata": "node scripts/check-release-metadata.mjs",
		"verify:standard-schema": "node scripts/check-standard-schema-boundaries.mjs",
		"prepare:offline-verification-cache": "node scripts/prepare-offline-verification-cache.mjs",
		"verify:sandbox-packages": "node scripts/check-sandbox-packages.mjs",
		"verify:production-adapters": "node scripts/check-production-adapter-packages.mjs",
		"build": "npm run build --workspaces --if-present",
		"ci": "npm run verify:architecture && npm run verify:standard-schema && npm run verify:release-metadata && npm run build && npm run lint && npm run verify:sandbox-packages && npm run verify:production-adapters && npm test && npm run test:coverage && npm run test:types && npm run test:contracts && npm run test:integration && npm run test:failure"
	},
	"allowScripts": {
		"@google/genai@2.20.0": false,
		"esbuild@0.28.2": true,
		"node-liblzma": false,
		"protobufjs": false,
		"@mongodb-js/zstd": false,
		"fsevents": false
	},
	"devDependencies": {
		"@napi-rs/cli": "3.8.6"
	}
}
