# CTR-DB-CLEANUP — clean cut

The owner explicitly permits breaking changes and prohibits legacy/compatibility/migration work. Remove onPermission/OnPermission/PermissionContext/PermissionDecision and permission ask mode; old GovernanceRiskLevel/GovernanceAuditContext exports and governance result prose/metadata/identity fields; old interceptor/policy/guardrail evaluation error classes; ProviderItems/providerItems; review consumed/consumeApproved; duplicate guardrail timer/abort/evidence/JSON validators and replay paths that retain original arguments. Delete obsolete tests and examples only when replaced by positive new behavior and negative removed-API tests. No deprecated exports, overload bridges, converters, fallback readers, feature flag or dual behavior.

`check-decision-boundaries.mjs` scans runtime source, tests excluding explicit negative fixtures, exports, current docs/examples/skills and targeted PURISTA Core, starter and create-purista consumer surfaces. Voyage is excluded. Exact old identifiers fail; permission ask is checked in permission definitions/usages, not unrelated English prose. Historical plans and superseded spec removal inventories are excluded. Type tests assert removed APIs are rejected; runtime tests assert old callback fields/shapes reject, not silently normalize. The checker verifies public-only addon imports, absence of a second decision timer helper and expected modules/exports; behavioral tests remain the authority for sequencing and privacy. Add node:test fixtures proving each scanner rule fails on a regression and passes on a valid file. Do not ban all timers, all unknown ingress or unrelated metadata fields.

Keep one source for closed schemas/types and one primitive implementation. Review changed code for duplicate mappers/reducers, wrapper layers without consumers, redundant casts, dead exports/helpers, abandoned imports/tests and source/generated drift. Reuse existing build declarations, capability catalog and skill sync. No production implementation may be replaced by a noop/fake. Synthetic fixtures are test-only; the documented payment reference remains explicitly an application reference.

Final evidence covers Harness lint/build/tests/type tests/storage contracts/failure/integration suites, all changed providers/addon suites, consumer check, PURISTA focused tests and skills/knowledge/handbook/API audits. Existing coverage policies apply without new percentages. Capture failures rather than narrowing global commands to hide them. Do not run broad formatter fixes or delete unrelated work. No version bump, package publishing, git commit, push, data reset or registry install is part of these tickets.

The root build includes the existing native privacy package; run it with CARGO_NET_OFFLINE=true. Missing cached native dependencies block verification rather than permit network downloads. No source or cache reset is authorized.
