# CTR-DB-CONSUMERS — integration inventory

Implement Harness contracts first. PURISTA Core consumes only public Harness exports and continues to own service authorization, queue/run idempotency and resource scoping; no second policy/approval engine. Update `purista/packages/core/src/AgentQueueBuilder/types.ts`, runtime/executor.ts, runtime/scopedRuntime.ts, runtime/sseEvents.ts, testing/index.ts and their existing tests for changed governance types and safe event payloads, including model.completed. Preserve unrelated queue semantics.

Starter and create-purista currently declare no affected governance hooks or rail adapters. Verify their source/template scan remains empty for removed API symbols, and update only an actually present affected Harness consumer; no new approval scaffold. Voyage, including its source, documentation, compiler configuration, dependencies and application review workflow, is outside this workstream and must not be read, scanned, compiled, changed or used as an acceptance gate.

Add `ai-harness/scripts/verify-decision-consumers.mjs` as hermetic coordinator, with node:test fixtures for dependency resolution and failure propagation. It accepts workspace root (default parent of ai-harness) and optional `--check`. It creates only `ai-harness/.artifacts/decision-consumers/`, generates temporary TypeScript configs and Vitest alias overlays from existing repository configs, and uses installed local TypeScript/Vitest executables. Generated check configs set incremental:false and noEmit:true; declaration smoke sets skipLibCheck:false. No inherited build-info path can write outside the owned artifact directory. Overlay exact @purista/harness, @purista/harness/testing, @purista/harness-openai and @purista/core imports to this workspace's source; preserve all existing other aliases. Compile Core AgentQueueBuilder consumers with noEmit and run existing Core agentQueueBuilder/scopedRuntime tests under those aliases. Build-package smoke separately imports new core/addon/provider exports from freshly built local dist and compiles a small consumer against their declarations using the same exact local mappings. Assertions must prove resolved paths point to current workspace source/dist, never registry-installed prior versions. `--check` runs the same verification without changing source; generated temp artifacts may be refreshed. Clean up only its own generated directory on success; retain diagnostics on failure. It must not edit package manifests/lockfiles, install/link node_modules, start an application, or use credentials/network. Missing in-scope dependencies or declaration failures are blockers with exact evidence, not skipLibCheck/cast workarounds. The script also scans starter/create-purista for removed symbols, excluding prose and installed/generated directories. Registry version coordination is a later authorized release, not an implementation compatibility mechanism.

# CTR-DB-DOCS — current developer journey

Update Harness docs/reference/public-api.md and docs/guides/guardrails.md/configuration.md, root/package READMEs, relevant examples and `skills/ai-harness` canonical references; Harness has no packaged skill mirror: run its existing sync/check script against ai-harness/.artifacts/decision-skills/ai-harness to verify reproducible copying; do not write installed user skill directories. Add only exact decision-consumers/ and decision-skills/ subdirectory ignore entries to ai-harness/.gitignore. In PURISTA update canonical `skills/purista/references/05-ai-harness-runtime.md` and evaluation scenarios, then run `node purista/scripts/syncPackageSkills.mjs purista/packages/core` from workspace root; never omit the target. Public docs must stand alone without internal spec access.

Update existing handbook source `purista/web/src/content/handbook/harness/secure-and-govern/guardrails.md`, the Harness markdown data source, relevant guardrails-governance/human-review-gates/observability-operations/ecosystem-packages cards under web/src/content/handbook-cards/harness and handbook-cards/blocks/agent-pattern/guardrails.mdx. Preserve routes/layout. Apply web/AGENTS.md and DESIGN.md, existing docs components and canonical docs-maintainer guidance; no UI rebuild or information-architecture migration.

Teach one decision table: content allow/block/phase transform; permission/policy allow/deny/audit/require_approval as applicable; immediate approved/rejected; durable ExternalWaitOutcome plus application claim. Show composed input/tool/output rails and governance on one agent, static permission approval with the same provider, correlated multi-tool policy narrowing, bounded signal/deadline, safe reason codes, cancellation and fail-closed errors. Use bank-governance as the runnable immediate-approval example and durable-human-review for wait/claim/receipt. Explain parsed input versus effective wire arguments, final-only output rails, direct model-call limits, opaque reasoning limits and absence of post-admission revocation. Show review state in application storage without introducing core reviewer CRUD.

Docs cleanup must remove old hook/prose result examples and legacy guides, not preserve both recipes. Add typechecking for new TS snippets through existing example/type-test fixtures; do not copy a second untested implementation into prose. Run PURISTA audit:skills, audit:knowledge, audit:handbook and audit:api-docs. Do not run update:doc because it stages/commits. Produce concise release breaking-change notes, not migration or compatibility instructions.

Verification ordering is mandatory: rebuild Harness before addon/example checks that resolve its dist; rebuild guardrails before detector/example checks that resolve addon dist. Provider packages run their existing typecheck scripts as well as Vitest. Add guardrails tsconfig.type-tests.json (extends build config; noEmit:true, incremental:false, rootDir:dot; includes src/**/*.ts and type-tests/**/*.ts) and test:types script; negative assertions belong in type-tests/guardrails-typing.ts. Do not widen the production compiler rootDir. Example documentation gates run both test and typecheck. Core focused tests run only through the local-source consumer overlay, never an additional raw PURISTA command resolving old installed Harness. Root full build precedes lint/typecheck to refresh local dependency declarations.
